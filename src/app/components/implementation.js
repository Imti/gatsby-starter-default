import React from 'react';

function Implementation(props) {
    const { name, statuses, features } = props;

    return (
        <div>
            <div>{ name }</div>
        </div>
    );
}

export default Implementation;
